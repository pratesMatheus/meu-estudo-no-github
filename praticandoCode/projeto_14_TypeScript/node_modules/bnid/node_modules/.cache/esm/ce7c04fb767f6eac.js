let crypto,promisify;_767‍.x([["getRandomBytes",()=>getRandomBytes],["bytesToHex",()=>bytesToHex],["bytesFromHex",()=>bytesFromHex]]);_767‍.w("crypto",[["*",null,function(v){crypto=v}]]);_767‍.w("util",[["promisify",["promisify"],function(v){promisify=v}]]);// Node.js support



const randomFill = promisify(crypto.randomFill);

       async function getRandomBytes(buf) {
  return randomFill(buf);
}

       function bytesToHex(bytes) {
  return Buffer.from(bytes).toString('hex');
}

       function bytesFromHex(hex) {
  return Buffer.from(hex, 'hex');
}
